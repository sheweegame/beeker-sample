name: 1. add-stats-layout
label: '[Page] [Stats] - Add stats layout to page'
description: Allow to add new stats layout
group: '[Page] [Stats] - Add'

variables:
  - name: url
    label: Page url
    default_value: /dashboard

rules:
  - name: add-stats-layout-to-page
    path: ./add-stats-layout-to-page.beek

files:
  - path: ../../../../../../app{{url}}/page.tsx
    rules:
      - add-stats-layout-to-page
