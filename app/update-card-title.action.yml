name: update-card-title
label: Dashboard - Update title in card
description: Allow to update title in a card summary (like Website for example)
group: Dashboard - Card

variables:
  - name: oldName
    label: Old name
  - name: newName
    label: New name

rules:
  - name: update-card-title
    path: ./update-card-title.beek

files:
  - path: ./page.tsx
    rules:
      - update-card-title
