name: add-chart-data
label: Dashboard - Add data in chart
description: Allow to add data in chart
group: Dashboard - Chart

variables:
  - name: month
    label: Month
  - name: sales
    label: Sales
  - name: profit
    label: Profit

rules:
  - name: add-chart-data
    path: ./add-chart-data.beek

files:
  - path: ./chart.tsx
    rules:
      - add-chart-data
