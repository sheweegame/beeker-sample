name: add-bar-data
label: Dashboard - Add bar in card
description: Allow to add a new bar in a card summary (like Website for example)
group: Dashboard - Card

variables:
  - name: targetCardTitle
    label: Target card title
  - name: newBarName
    label: New bar name
  - name: newBarValue
    label: New bar value

rules:
  - name: add-bar-data
    target: ./page.tsx
